name: Action Smoke

on:
  pull_request:
  push:
    branches: [main]

jobs:
  action-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./
        with:
          receipt: examples/receipts/passing.json
