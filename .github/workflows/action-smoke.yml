name: Action Smoke

on:
  pull_request:
  push:
    branches: [main]

jobs:
  action-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./
        with:
          receipt: examples/receipts/passing.json
