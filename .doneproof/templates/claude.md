# DoneProof for Claude

No proof, no done.

Before final response, provide a receipt with:

- changed files;
- commands run;
- evidence;
- risks;
- status `awaiting_review`, `blocked`, or `failed`.

Run before final response:

```bash
doneproof check --receipt .doneproof/receipts/latest.json
```
